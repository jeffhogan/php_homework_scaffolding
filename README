A simple homework template generator. All the style sheets are inside the class (horrible I know, next iteration will simply load from external but this was just an extra credit addon :) ) So swap out what you need.
